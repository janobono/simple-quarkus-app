quarkus:
  datasource:
    db-kind: mssql
    jdbc:
      url: jdbc:sqlserver://localhost:1433;database=room;integratedSecurity=false;encrypt=false;trustServerCertificate=true;
    username: room
    password: room

  liquibase:
    migrate-at-start: true

  log:
    category:
      "sk.janobono":
        level: DEBUG
    level: INFO

  http:
    root-path: /api

"%dev":
  quarkus:
    http:
      port: 8084
